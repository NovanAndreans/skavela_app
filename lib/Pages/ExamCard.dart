import 'package:flutter/material.dart';

class ExamCardPage extends StatelessWidget {
  const ExamCardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Generate Kartu Ujian'),
    );
  }
}