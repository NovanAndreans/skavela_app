import 'package:flutter/material.dart';

class DeskCardPage extends StatelessWidget {
  const DeskCardPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Generate Kartu Meja'),
    );
  }
}